import {MaterialsRepository} from "./materials-repository";

describe("MaterialsRepository", () => {
  it("should create an instance", () => {
    expect(new MaterialsRepository()).toBeTruthy();
  });
});
